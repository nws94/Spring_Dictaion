server:
  port: 3003
spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/DB이름
    driver-class-name: org.mariadb.jdbc.Driver
    username: 사용자이름
    password: 비밀번호

mybatis:
 mapper-locations: classpath:mapper/**/*.xml
 type-aliases-package: com.dictation.vo